{
  "name": "qzone-ruffle-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Headless Ruffle render check: which scraped SWFs actually play?",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "render": "node render.mjs"
  },
  "dependencies": {
    "@ruffle-rs/ruffle": "^0.2.0",
    "playwright": "^1.49.0"
  }
}
